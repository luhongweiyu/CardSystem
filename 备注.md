按你的容忍度，我同意软件删除取消大事务。
建议顺序：
1. 先删除 software，阻止后续正常发卡和配置操作；
2. 删除该软件的设备会话；
3. 删除卡密；
4. 删除计费方案；
5. 无论中间是否报错，最后都清除心跳缓存和软件计费配置缓存；
6. 每一步错误只记录日志，接口返回哪些清理失败，允许再次删除重试。
不需要删除前清缓存，也不需要重复整个删除流程。正常情况下最多只是删除过程中的个别心跳继续成功一次。
极端情况下某一步删除失败且并发请求重新建立缓存，最多延续到当前 authorized_until，目前最长 3 天，不会永久白嫖。