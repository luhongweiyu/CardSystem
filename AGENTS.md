# CardSystem AI 协作指引

## 适用范围

- 本文件适用于整个仓库。
- 项目代码和业务文档主要位于 `前端`、`后端`；只有任务明确需要时才修改根目录中的项目配置或文档。
- `其他与项目无关文件` 不属于项目范围，不读取、不修改、不整理、不提交其中的内容。
- 未经明确要求，不处理压缩包、截图、数据库备份、构建产物等非源码文件。
- 不随意改变已有文件的编码、换行符或格式，不进行与当前需求无关的全文件格式化。

## 工作方式

1. 先阅读本文件，再根据需求按需读取相关代码，避免无目的地扫描整个仓库。
2. 后端业务或接口变更优先查阅 `后端/README.md`、`后端/点卡API.md` 和对应 Go 文件。
3. 前端变更优先查阅 `前端/README.md`、相关页面、状态管理和请求封装。
4. 使用搜索定位调用关系和旧字段残留；涉及共享模型、事务或接口契约时，检查所有相关调用方。
5. 不猜测业务规则。现有说明不足时，先说明合理假设；假设会明显改变业务结果时，先向用户确认。
6. 用户最新明确提出的需求优先；业务规则发生变化时，同步更新受影响的 README 或 API 文档。
7. 保持改动范围紧凑，不顺带重构无关代码，也不为了小问题引入复杂抽象。
8. 未经用户明确要求，不创建 Git 提交，不修改或删除用户已有的未提交改动。

## 不可破坏的核心业务规则

- 系统现在同时支持点卡和时长卡两种独立模式：点卡不使用时长卡字段；时长卡使用独立的
  `duration_card_<管理员名>` 表保存固定时长、激活和到期信息。不得把两种卡密字段混入同一张表。
- 点卡当前余额的唯一来源是所属管理员 `point_card_<管理员名>` 表中的 `point_balance`。
- 点卡允许多设备使用；同一管理员、卡密、`device_id` 组合只能有一条设备会话，软件编号始终从点卡记录读取。
- 点卡的 `device_id` 可选，省略时统一按空字符串处理，登录与心跳必须保持一致；非空值应由客户端持久化。`device_alias` 仅在登录时可选且允许重复；`needle` 是服务端随机生成的心跳校验令牌，不是设备 ID。
- 点卡同一设备授权未到期时重复登录不扣点。未到期时切换授权时长，只更新下一次续费时长，不立即扣点或修改当前授权截止时间。
- 点卡和时长卡的授权期内普通心跳只更新各自的进程内心跳缓存；单条脏缓存按配置的 15 至 60 分钟间隔由下一次心跳同步数据库，不使用定时扫描。登录、续费、退出、冻结、暂停、恢复、充值、删除和软件变更会同步并失效相关缓存；点卡后台每分钟清理或续费到期会话。
- 点卡在线推断使用 `last_heartbeat_at + online_grace_minutes × 1分钟`；该值按软件配置，设置为 0 时默认 60 分钟，非零值必须为 5 至 4320 分钟，管理端将其显示为“自动离线时间”。仍在线时从旧授权截止时间续费，已停止时从当前时间重新计费。
- 点卡授权时长按软件已启用的计费方案执行；自动离线时间独立于心跳间隔和计费周期。
- 点卡计费周期最小 5 分钟、最大 3 天；管理端和客户端接口按分钟使用，数据库内部继续按秒保存并在接口边界换算。
- 点卡退出接口直接删除 `point_device_session`，不保存退出状态，也不产生点数流水。
- 点卡每次真实余额变化必须写入一条 `point_ledger`；没有余额变化时不得写流水。设备 ID 和别名快照合并写入 `remark`。
- 点卡余额更新、流水写入和会话授权更新必须保持在同一事务中；并发扣费继续遵循现有锁顺序，防止重复扣点和死锁。
- 删除点卡时删除当前设备会话；点数流水仅保留最近 30 天，由后台定时清理。同名点卡允许重新生成，保留期内的新旧流水允许按卡密名混合保存。
- 时长卡自身保存固定分钟数和 `end_time`；首次登录激活，重复登录只刷新服务端 `needle`，不延长到期时间。同一张时长卡同时只接受最后一次登录的 needle。
- 时长卡暂停费用按软件的 `pause_deduct_minutes` 设置，`0` 表示关闭；暂停时保存扣费后的剩余分钟并清空到期时间，只有恢复接口可以重新启用。
- 已激活或暂停中的时长卡可以使用同软件、未激活且状态正常的另一张时长卡充值；暂停卡只累加 `paused_remaining_minutes`。来源卡成功后永久标记为已用于充值，不得恢复使用。
- 独立时长充值卡保存在 `duration_recharge_card`，按单次充值分钟数和剩余次数使用；代理生成充值卡时，余额扣减与充值卡创建必须处于同一事务。
- 时长卡和点卡允许使用相同卡密文本，但客户端必须明确调用 `/duration_card/*` 或 `/point_card/*`，服务端不得跨表猜测模式。
- 管理员和代理的点卡管理接口统一使用 `/admin/point_card/*`、`/agent/point_card/*`；旧客户端路径 `/card/*`、`/duration/*` 不在新服务端注册。

## 账号与代理规则

- 管理员和代理账号按现有要求同时保存 `password` 原值与 `password_hash`。
- 登录只比较 `password_hash`，不得使用 `password` 回退；哈希为空或 `NULL` 时仅该账号登录失败。
- 不自动补哈希，不额外增加旧密码修复逻辑。
- 所有接口都不得返回 `password` 或 `password_hash`。
- 统一使用“代理账号”称呼，不重新引入“子账号”概念。
- 代理拥有独立余额和各软件每点价格；代理价格中没有的软件视为未授权。
- 点卡代理价格继续使用点卡专用配置；时长卡代理发卡使用独立的 `duration_card_agent_price` 锚点表，不得直接复用点卡每点价格。精确命中锚点使用原价，锚点之间取较高平均单价折算，超出启用锚点范围拒绝生成。
- 代理生成卡密时，扣减代理余额与创建卡密必须在同一数据库事务中完成。
- 管理员创建代理接口继续使用 `agent_name`、`agent_password`，避免与管理员认证字段冲突。

## 数据库与部署边界

- 当前继续使用每个管理员独立的 `point_card_<管理员名>` 点卡表和 `duration_card_<管理员名>` 时长卡表；旧 `card_<管理员名>` 仅作为迁移来源，不在运行时读取。不擅自改为统一卡表、分区或分库。
- `AutoMigrate` 只创建或补充表结构，不负责旧卡密数据迁移。
- 项目不自动迁移旧业务数据；旧时长卡规则只能按独立新表重写，不得把旧字段直接塞回点卡表。
- 系统支持 HTTP，不得强制 HTTPS；安全提示应明确接口口令不能替代传输加密。
- 动态表名、外部输入、日志和流水备注必须继续经过现有校验，数据库错误必须向上返回并正确回滚。

## 代码与文档要求

- 代码必须可读、可维护、可直接运行，并沿用当前项目的目录、中文命名和职责划分。
- 函数按“入口/公开逻辑、核心业务、辅助函数”的清晰顺序放置；优先把逻辑放入现有职责对应的文件。
- 新增或修改的业务代码必须补充详细且有效的中文注释，重点解释业务条件、事务边界、并发约束和异常分支；不要写重复代码字面含义的空注释。
- 命名约定：明确属于点卡的类型和函数使用“点卡”前缀，时长卡使用“时长卡”前缀；两种模式共用的认证、签名和卡密文本辅助函数保留“卡密”或“卡端”等中性命名。内部重命名不得改变接口路径和 JSON 字段。
- 接口字段、默认值、响应语义或业务规则变化时，同步更新 `后端/点卡API.md` 及相关 README。
- 前端展示与后端接口含义保持一致，不保留已废弃概念的兼容 UI 或字段。
- 发现文档与代码不一致时，不静默选择其中一方；先结合测试和最新需求判断，无法确定则说明冲突并询问用户。

## 验证要求

- Go 文件改动后，先对改动文件执行 `gofmt`。
- 后端常规验证在 `后端` 目录执行：`go test ./...`、`go build ./...`、`go vet ./...`。
- 涉及计费、事务、锁、会话清理或其他并发路径时，再执行 `go test -race ./...`。
- 前端改动在 `前端` 目录至少执行 `npm run build`。
- 仅文档改动无需运行前后端构建，但仍应检查内容与现有实现是否一致。
- 交付前在仓库根目录执行 `git diff --check`，并查看 `git status --short`，确保没有误改无关文件。
- 如果某项验证因环境或外部依赖无法执行，必须明确说明未验证的项目和原因。
