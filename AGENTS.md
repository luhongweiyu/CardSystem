# CardSystem AI 协作指引

## 适用范围

- 项目代码和主要文档位于 `前端`、`后端`；`其他与项目无关文件` 不读取、不修改、不提交。
- 未经明确要求，不处理压缩包、截图、数据库备份、构建产物和根目录其他无关文件。
- 不随意修改文件编码、换行符或无关格式，不创建 Git 提交，除非用户明确要求。

## 工作方式

1. 先按需求读取相关代码和文档，不无目的扫描整个仓库。
2. 后端任务优先看 `后端/README.md`、`后端/点卡API.md` 和对应 Go 文件；前端任务优先看 `前端/README.md`、相关页面和请求封装。
3. 使用搜索确认调用关系、旧字段和接口路径，涉及共享模型、事务或并发时检查所有调用方。
4. 不猜测业务规则；最新用户要求优先于旧文档、旧实现和历史方案。
5. 改动保持紧凑，不顺带重构无关代码；接口或规则变化时同步更新对应文档。
6. `AGENTS.md` 只记录长期稳定、不可违反的项目规则；小修改、临时兼容方案和阶段性决定写在对应代码注释中，不为每次小改扩充本文件。

## 架构与业务边界

- 点卡和时长卡是两套独立模式：点卡使用 `point_card_<管理员名>`，时长卡使用 `duration_card_<管理员名>`，不得混用字段或跨表猜测模式。
- 旧 `card_<管理员名>` 只作为迁移来源，不在正式业务中读取。只有配置 `迁移: true` 时才执行迁移，成功后备份为 `_bak`。
- 新客户端接口使用 `/point_card/*`、`/duration_card/*`；管理端和代理端点卡接口使用 `/admin/point_card/*`、`/agent/point_card/*`。仅保留旧 `/card/card_login`、`/card/card_ping` 给旧时长卡客户端，其他旧路径不注册；这两个旧接口使用旧签名协议。
- 点卡余额以所属管理员点卡表的 `point_balance` 为准；设备会话保存在 `point_device_session`，流水保存在 `point_ledger`。
- 点卡允许多设备，同一管理员、卡密、`device_id` 只能有一条会话。`device_id` 省略时按空字符串处理，登录和心跳必须一致；`device_alias` 只在登录时使用；`needle` 由服务端生成。
- 点卡普通心跳使用进程内缓存，达到配置间隔后由下一次心跳同步；登录、续费、退出、冻结、删除、充值、暂停、恢复和软件变更等操作必须先同步并使相关缓存失效。
- 点卡扣余额、写流水和更新授权会话必须在同一事务中完成；没有真实余额变化时不得写流水。后台任务不得形成不必要的大事务。
- 时长卡保存固定时长和到期时间；首次登录激活，重复登录不重复延长。暂停、恢复、续费和充值遵循现有时长卡业务，暂停卡充值累加暂停剩余分钟。
- 管理员和代理同时保存密码原值与哈希；登录只比较哈希，哈希为空时登录失败；接口不得返回任一密码字段。
- 支持 HTTP，不强制 HTTPS。接口签名不能替代传输加密；动态表名和外部输入必须继续校验。

## 文档与代码要求

- 代码沿用现有 Go、Gin、GORM、Vue 结构和中文命名；明确属于点卡或时长卡的函数、类型使用对应前缀。
- 新增或修改的关键业务逻辑补充必要中文注释，重点说明事务、并发和异常边界，不写重复代码含义的空注释。
- `后端/点卡API.md` 负责接口参数、响应和客户端契约；README 负责运行、部署和结构说明；不要把完整接口手册复制到 `AGENTS.md`。
- 发现代码与文档不一致时，先结合测试和最新需求判断；无法确定时说明冲突，不静默选择。

## 验证

- Go 改动后先执行 `gofmt`；后端在 `后端` 目录执行 `go test ./...`、`go build ./...`、`go vet ./...`。
- 涉及计费、事务、锁、会话或缓存并发时再执行 `go test -race ./...`。
- 前端改动至少执行 `npm run build`。
- 交付前在根目录执行 `git diff --check` 和 `git status --short`，确认没有误改无关文件。
